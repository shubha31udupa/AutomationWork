package UIPages;

import org.openqa.selenium.WebDriver;

/**
 * Dash board page. Only public class used in POM since it is first page of application.
 * @author shubha
 *
 */
public class dashboardPage extends basePage {

	public dashboardPage(WebDriver driver)
	{
		super(driver);
	}
}
